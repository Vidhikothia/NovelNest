@model IEnumerable<BookStore.Models.Domain.Publisher>
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-primary btn-lg" href="/Publisher/Add">Add Publisher</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Publishers</h3>

            <table  class="table table-stripped table-bordered table-hover">
                <thead action="thead-dark">
                    <tr>
                        <th>PublisherName</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model){
                    <tr>
                        <td>@item.PublisherName</td>
                        <td>
                                <a class="btn btn-success" href="/Publisher/Update?id=@item.Id">Edit</a>
                                <a class="btn btn-danger" onclick="return window.confirm('Are you sure?')" href="/Publisher/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


<style>
    /* Additional Custom Styles */
    .table th, .table td {
        vertical-align: middle; /* Center content vertically */
    }

    .btn {
        margin-right: 10px; /* Space between buttons */
    }

    .table-hover tbody tr:hover {
        background-color: #f1f1f1; /* Light background on hover */
    }

    .thead-dark th {
        background-color: #343a40; /* Dark header background */
        color: #ffffff; /* White text */
    }
</style>